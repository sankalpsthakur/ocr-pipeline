# Ultra-minimal CI dependencies - CPU only, no ML dependencies
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
black>=24.0.0
codecov>=2.1.0
coverage>=7.0.0

# Core dependencies for basic testing
pillow>=10.0.0
pytesseract>=0.3.10
pdf2image>=1.17.0
pdfminer.six>=20221105
requests>=2.32.0
numpy>=1.24.0

# Exclude ALL ML dependencies that require compilation:
# - easyocr (requires torch)
# - opencv-python-headless (requires compilation)
# - paddlepaddle (96MB)
# - torch/torchvision (GPU dependencies)
# - mistralai dependencies
# - langchain ecosystem